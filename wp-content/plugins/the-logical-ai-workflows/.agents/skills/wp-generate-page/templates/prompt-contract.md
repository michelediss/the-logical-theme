# Prompt Contract

- Work on one page only.
- Read the whitelist before any code reasoning.
- Use `figma-make-architecture.md` only as interpretation support.
- Produce explicit rationale for every section-to-block mapping.
- Fail explicitly when a section cannot be expressed with allowed blocks.
